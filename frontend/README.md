# Frontend

This is the Next.js frontend for FitLetter.

Run from this directory:

```bash
npm install
npm run dev
```

Main UI entry point: `app/page.tsx`.

For full project setup, see the root README:
`../README.md`.
